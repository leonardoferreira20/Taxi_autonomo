#include "Settings.h"

int main(int argc, char * argv[]){
  int kms = 0;


  if (argc!=2){
    perror("[sistema] Numero invalido de parametros!\n");
    exit(-1);
  }

  /* RECEBE POR ARGUMENTO O LOCAL QUE É O MAX DE DISTANCIA A PERCORRER AQUI SERIA UM ARGV METI ARGV[3] mas pode se outro
  while(km!= argv[3]){
    ++km;
    sleep (1);
  }
  */


  return 0;
}